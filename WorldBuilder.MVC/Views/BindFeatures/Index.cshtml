@model IEnumerable<WorldBuilder.Data.PCtoFeatureBinding>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Feature.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PC.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Feature.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PC.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PCtoFEBindID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PCtoFEBindID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PCtoFEBindID })
        </td>
    </tr>
}

</table>
